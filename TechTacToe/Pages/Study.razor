@page "/study"

<PageTitle Title="Study Questions"></PageTitle>

<StudyEntry Question="What is a common spindle speed for magnetic hard drives?" GoodResponses="5400, 7200, 10000, 15000"></StudyEntry>

<StudyEntry Question="What is a common SSD form factor?" GoodResponses="1.8, 2.5, 3.5"></StudyEntry>

<StudyEntry Question="What connection types SSD use?" GoodResponses="SATA, PATA, eSATA, USB"></StudyEntry>

<StudyEntry Question="What is an HHD?" GoodResponses="hybrid hard drive"></StudyEntry>

<StudyEntry Question="What type of power connector do PATA drives use?" GoodResponses="Molex"></StudyEntry>

<StudyEntry Question="What is an ATA?"  GoodResponses="Advanced Technology Attachment" ></StudyEntry>

<StudyEntry Question="What is S.M.A.R.T?"  GoodResponses="Self Monitoring and Reporting Technology" ></StudyEntry>

<StudyEntry Question="What is max throughput for SATA 1.0?"  GoodResponses="1.5 Gbps, 1.5 gigabits" ></StudyEntry>

<StudyEntry Question="What is max throughput for SATA 2.0"  GoodResponses="3 Gbps, 3 gigabits" ></StudyEntry>

<StudyEntry Question="What is max throughput for SATA 3.0"  GoodResponses="6 Gbps, 6 gigabits" ></StudyEntry>

<StudyEntry Question="What is max throughput for SATA 3.2"  GoodResponses="16 Gbps, 16 gigabits" ></StudyEntry>


<StudyEntry Question="What is AHCI?"  GoodResponses="Advanced Host Controller Interface" ></StudyEntry>

<StudyEntry Question="You should enable AHCI after you install OS?"  GoodResponses="false" ></StudyEntry>

<StudyEntry Question="What features does AHCI unlock?"  GoodResponses="hot swapping, native command queing" ></StudyEntry>

<StudyEntry Question="What is NCQ?"  GoodResponses="Native Command Queing, NCQ" ></StudyEntry>

<StudyEntry Question="NCQ is a disk optimization feature for what type for drive?"  GoodResponses="SATA" ></StudyEntry>

<StudyEntry Question="What is RAID?"  GoodResponses="Redundant Array of Independent Disks" ></StudyEntry>

<StudyEntry Question="What is RAID 1 used for?"  GoodResponses="disk mirroring" ></StudyEntry>

<StudyEntry Question="What is RAID 0 used for?"  GoodResponses="disk striping" ></StudyEntry>

<StudyEntry Question="What level of RAID is disk striping with parity?"  GoodResponses="5" ></StudyEntry>

<StudyEntry Question="How many drives do you need to implement RAID 5?"  GoodResponses="3" ></StudyEntry>

<StudyEntry Question="What is JBOD?"  GoodResponses="Just a bunch of disks" ></StudyEntry>

<StudyEntry Question="How many functional failures does RAID 5 have?"  GoodResponses="1" ></StudyEntry>

<StudyEntry Question="How many functional failures does RAID 6 have?"  GoodResponses="2" ></StudyEntry>

<StudyEntry Question="How many drives do you need to implement RAID 6?"  GoodResponses="5" ></StudyEntry>

<StudyEntry Question="What RAID level uses extra parity?"  GoodResponses="6" ></StudyEntry>

<StudyEntry Question="What is striped mirrors?"  GoodResponses="RAID 10" ></StudyEntry>

<StudyEntry Question="How many functional failures does mirror of stripes have?"  GoodResponses="2" ></StudyEntry>

<StudyEntry Question="How many drives do you need to implement RAID 10?"  GoodResponses="4" ></StudyEntry>

<StudyEntry Question="What is the RAID implementation available in Windows 8/8.1/10 called?"  GoodResponses="storage spaces" ></StudyEntry>

<StudyEntry Question="What handles all the work in hardware RAID implementation?"  GoodResponses="Intelligent controller,controller" ></StudyEntry>

<StudyEntry Question="Hardware RAID controllers have their own ____ so they can handle all RAID work?"  GoodResponses="processor, memory" ></StudyEntry>

<StudyEntry Question="What is a common concern when using RAID?"  GoodResponses="heat" ></StudyEntry>

<StudyEntry Question="What are some common case fan sizes?"  GoodResponses="60,80,120" ></StudyEntry>

<StudyEntry Question="What is used to indicate pin 1 on hard drive cable?"  GoodResponses="colored stripe, stripe" ></StudyEntry>

<StudyEntry Question="What do you use to install a 2.5 inch SSD in desktop 3.5 inch bay?"  GoodResponses="form factor adaptor, adaptor" ></StudyEntry>

<StudyEntry Question="What SSD form factor is typically used in laptops?"  GoodResponses="2.5, 2.5 inch, 2.5 in" ></StudyEntry>

<StudyEntry Question="What does formatting a drive create?"  GoodResponses="file system, root directory, root folder" ></StudyEntry>

<StudyEntry Question="What is a partitioning method used by a basic disk?"  GoodResponses="GPT, MBR" ></StudyEntry>

<StudyEntry Question="What partition method is used by a dynamic disk?"  GoodResponses="dynamic storage, dynamic storage partitioning" ></StudyEntry>

<StudyEntry Question="What loads the OS in a MBR drive?"  GoodResponses="partition boot sector, boot sector" ></StudyEntry>

<StudyEntry Question="What type of partition(s) does a MBR drive support?"  GoodResponses="primary, extended" ></StudyEntry>

<StudyEntry Question="True or False. You can boot OS from an extended partion?"  GoodResponses="false" ></StudyEntry>

<StudyEntry Question="Where does Windows store essential Windows boot files?"  GoodResponses="System reserved, system reserved partition" ></StudyEntry>

<StudyEntry Question="A logical drive gets a drive letter?"  GoodResponses="true" ></StudyEntry>

<StudyEntry Question="What is the drive structure in a dynamic disk called?"  GoodResponses="volume" ></StudyEntry>

<StudyEntry Question="How many volumes can a dynamic disk have?"  GoodResponses="infinite, unlimited, no limit, alot" ></StudyEntry>

<StudyEntry Question="Simple volumes work a lot like what type of partition?"  GoodResponses="primary" ></StudyEntry>

<StudyEntry Question="What type of motherboard do you need to boot modern Windows of MacOSX from GPT?"  GoodResponses="UEFI" ></StudyEntry>

<StudyEntry Question="What is the status code for a bad sector in FAT location?"  GoodResponses="FFF7" ></StudyEntry>

<StudyEntry Question="What is the status code for a good sector in FAT location?"  GoodResponses="0000" ></StudyEntry>

<StudyEntry Question="What does full formatting do (look for) that quick formatting does not?"  GoodResponses="bad sectors" ></StudyEntry>

<StudyEntry Question="What is it called when you combine a set of contiguous sectors and treat them as a single unit?"  GoodResponses="clustering, cluster" ></StudyEntry>

<StudyEntry Question="What status code indicates end of file in FAT?"  GoodResponses="FFFF" ></StudyEntry>

<StudyEntry Question="What is it called when a file gets written to a separate arbitrary place on disk?"  GoodResponses="fragmentation" ></StudyEntry>

<StudyEntry Question="How often should you defrag an SSD?"  GoodResponses="never" ></StudyEntry>

<StudyEntry Question="What size of partitions does FAT16 support?"  GoodResponses="2GB,2 gigabytes" ></StudyEntry>

<StudyEntry Question="What size of partitions does FAT32 support?"  GoodResponses="2TB, 2 terabytes" ></StudyEntry>

<StudyEntry Question="What size of partitions does NTFS support?"  GoodResponses="2TB, 2 terabytes, 16TB, 16 terabytes, 16EB, 16 exabytes" ></StudyEntry>

<StudyEntry Question="What is NTFS?"  GoodResponses="New Technology File Sytem" ></StudyEntry>

<StudyEntry Question="What improvements/features did NTFS introduce?"  GoodResponses="compression, encrytpion, disk quotas, cluster sizing, security, redundancy" ></StudyEntry>

<StudyEntry Question="What does NTFS keep a backup copy of in the middle of disk?"  GoodResponses="MFT, master file table" ></StudyEntry>

<StudyEntry Question="How do you provide security to files and folders in NTFS?"  GoodResponses="ACL, access control list" ></StudyEntry>

<StudyEntry Question="What is EFS?"  GoodResponses="Encrypted file system" ></StudyEntry>

<StudyEntry Question="What keeps users from monopolizing drive space?"  GoodResponses="disk quotas" ></StudyEntry>

<StudyEntry Question="What is the largest file size you can have on FAT32 drive?"  GoodResponses="4GB, 4 gigabytes" ></StudyEntry>

<StudyEntry Question="What is the largest file size  you can store on exFAT drive?"  GoodResponses="16EB, 16 exabytes" ></StudyEntry>

<StudyEntry Question="What is the partion size limit for exFAT drive(theoretically)?"  GoodResponses="64ZB, 64 zetabyes" ></StudyEntry>

<StudyEntry Question="What causes issues with NTFS on usb flash drives when switching systems?"  GoodResponses="encryption, ACL, access control list, permissions" ></StudyEntry>

<StudyEntry Question="What file system does Linux use?"  GoodResponses="ext4, Fourth Extended File System" ></StudyEntry>

<StudyEntry Question="What is the volume size limit on ext4 drive?"  GoodResponses="1EB, 1 exabyte" ></StudyEntry>

<StudyEntry Question="What is the file size limit on ext4 drive?"  GoodResponses="16TB, 16 terabytes" ></StudyEntry>

<StudyEntry Question="What is removable media with bootable OS called?"  GoodResponses="boot disk, boot device" ></StudyEntry>

<StudyEntry Question="What is every newly installed drive set as?"  GoodResponses="basic disk, basic" ></StudyEntry>

<StudyEntry Question="Is it a good idea to use SSD to implement parity spaces?"  GoodResponses="No" ></StudyEntry>

<StudyEntry Question="Is it ok to do two or three way mirror with SSD?"  GoodResponses="Yes" ></StudyEntry>

<StudyEntry Question="What are some tools for error checking a hard drive on Windows,MacOSX,Linux?"  GoodResponses="ScanDisk, chkdsk, Error-checking, Disk utility, fsck" ></StudyEntry>

<StudyEntry Question="How often should you do error checking on disks?"  GoodResponses="weekly"></StudyEntry>

<StudyEntry Question="How often should you defrag a drive?"  GoodResponses="monthly"></StudyEntry>

<StudyEntry Question="What could you lost if you dont' do error checking?'"  GoodResponses="data"></StudyEntry>

<StudyEntry Question="How often should you run disk cleanup in Windows?"  GoodResponses="monthly"></StudyEntry>

<StudyEntry Question="what is an allocation unit another name for?"  GoodResponses="cluster"></StudyEntry>

<StudyEntry Question="What reports back cluster errors?"  GoodResponses="S.M.A.R.T, smart"></StudyEntry>

<StudyEntry Question="What does the error 'trying to recover lost allocation unit' indicate?"  GoodResponses="dying drive"></StudyEntry>

<StudyEntry Question="What utility should you run first thing when you get windows error messages related to corrupt files, boot files, or missing files?"  GoodResponses="error-checking, error checking"></StudyEntry>

<StudyEntry Question="What feature in System Setup should you check in drive stops showing in explorer?"  GoodResponses="auto detect"></StudyEntry>

<StudyEntry Question=""  GoodResponses=""></StudyEntry>

<StudyEntry Question=""  GoodResponses=""></StudyEntry>

<StudyEntry Question=""  GoodResponses=""></StudyEntry>

<StudyEntry Question=""  GoodResponses=""></StudyEntry>

<StudyEntry Question=""  GoodResponses=""></StudyEntry>

<StudyEntry Question=""  GoodResponses=""></StudyEntry>

<StudyEntry Question=""  GoodResponses=""></StudyEntry>

@code
{

}